warning: in the working copy of 'Coral.Managed/Source/AssemblyLoader.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Coral.Native/Include/Coral/Assembly.hpp', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Coral.Native/Include/Coral/HostInstance.hpp', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Coral.Native/Source/Coral/Assembly.cpp', LF will be replaced by CRLF the next time Git touches it
diff --git a/Coral.Managed/Source/AssemblyLoader.cs b/Coral.Managed/Source/AssemblyLoader.cs
index 298a049..8455521 100644
--- a/Coral.Managed/Source/AssemblyLoader.cs
+++ b/Coral.Managed/Source/AssemblyLoader.cs
@@ -7,6 +7,7 @@ using System.IO.MemoryMappedFiles;
 using System.Reflection;
 using System.Runtime.InteropServices;
 using System.Runtime.Loader;
+using System.Threading;
 
 namespace Coral.Managed;
 
@@ -21,7 +22,8 @@ public static class AssemblyLoader
 {
 	// NOTE(Emily): Visible to `TypeInterface.cs`.
 	public static readonly Dictionary<int, AssemblyLoadContext?> s_AssemblyContexts = new();
-	private static Dictionary<int, string[]> s_AlcDllPaths = new();
+	private static readonly Dictionary<AssemblyLoadContext, int> s_AssemblyContextIds = new();
+	private static readonly Dictionary<int, string[]> s_AlcDllPaths = new();
 
 	private static readonly Dictionary<Type, AssemblyLoadStatus> s_AssemblyLoadErrorLookup = new();
 	private static readonly Dictionary<int, Dictionary<int, Assembly>> s_AssemblyCache = new();
@@ -32,6 +34,7 @@ public static class AssemblyLoader
 
 	private static readonly int CORAL_ALC_CACHE_ID = -1;
 	private static readonly AssemblyLoadContext? s_CoralAssemblyLoadContext;
+	private static int s_NextAssemblyLoadContextId;
 
 	static AssemblyLoader()
 	{
@@ -52,15 +55,44 @@ public static class AssemblyLoader
 	private static void CacheCoralAssemblies()
 	{
 		foreach (var assembly in s_CoralAssemblyLoadContext!.Assemblies)
+			CacheAssembly(CORAL_ALC_CACHE_ID, assembly);
+	}
+
+	private static int CacheAssembly(int InContextId, Assembly InAssembly)
+	{
+		if (!s_AssemblyCache.TryGetValue(InContextId, out var cache))
+			throw new InvalidOperationException($"AssemblyLoadContext '{InContextId}' has no assembly cache.");
+
+		int assemblyId = InAssembly.GetName().Name!.GetHashCode();
+		while (cache.TryGetValue(assemblyId, out var existing))
+		{
+			if (ReferenceEquals(existing, InAssembly))
+				return assemblyId;
+			assemblyId = assemblyId == int.MaxValue ? int.MinValue : assemblyId + 1;
+			if (assemblyId == -1)
+				++assemblyId;
+		}
+		cache.Add(assemblyId, InAssembly);
+		return assemblyId;
+	}
+
+	private static Assembly? FindCachedAssembly(int InContextId, string InAssemblyName)
+	{
+		if (!s_AssemblyCache.TryGetValue(InContextId, out var cache))
+			return null;
+		foreach (var assembly in cache.Values)
 		{
-			int assemblyId = assembly.GetName().Name!.GetHashCode();
-			s_AssemblyCache[CORAL_ALC_CACHE_ID].Add(assemblyId, assembly);
+			if (assembly.GetName().Name == InAssemblyName)
+				return assembly;
 		}
+		return null;
 	}
 
 	internal static bool TryGetAssembly(int InAssemblyLoadContextId, int InAssemblyId, out Assembly? OutAssembly)
 	{
-		return s_AssemblyCache[InAssemblyLoadContextId].TryGetValue(InAssemblyId, out OutAssembly);
+		OutAssembly = null;
+		return s_AssemblyCache.TryGetValue(InAssemblyLoadContextId, out var cache) &&
+			cache.TryGetValue(InAssemblyId, out OutAssembly);
 	}
 
 	internal static Assembly? ResolveAssembly(AssemblyLoadContext? InAssemblyLoadContext, AssemblyName InAssemblyName)
@@ -69,8 +101,6 @@ public static class AssemblyLoader
 		{
 			if (InAssemblyName.Name == null) throw new ArgumentNullException("InAssemblyName");
 
-			int assemblyId = InAssemblyName.Name.GetHashCode();
-
 			if (InAssemblyLoadContext == null || InAssemblyLoadContext.Name == null)
 			{
 				// Search all the assemblies!
@@ -92,12 +122,12 @@ public static class AssemblyLoader
 				return null;
 			}
 
-			int alcId = InAssemblyLoadContext.Name.GetHashCode();
-
-			if (s_AssemblyCache[alcId].TryGetValue(assemblyId, out var cachedAssembly))
-			{
+			int? alcId = s_AssemblyContextIds.TryGetValue(InAssemblyLoadContext, out int contextId)
+				? contextId
+				: null;
+			var cachedAssembly = alcId.HasValue ? FindCachedAssembly(alcId.Value, InAssemblyName.Name) : null;
+			if (cachedAssembly != null)
 				return cachedAssembly;
-			}
 
 			if (s_CoralAssemblyLoadContext != null)
 			{
@@ -106,10 +136,7 @@ public static class AssemblyLoader
 					if (assembly.GetName().Name != InAssemblyName.Name)
 						continue;
 
-					// NOTE(Emily): Disabling this doesn't seem to cause any problems -- but marking it as an
-					//              Unknown just in case.
-					//s_AssemblyCache[CORAL_ALC_CACHE_ID].Add(assemblyId, assembly);
-					return assembly;
+				return assembly;
 				}
 			}
 
@@ -120,7 +147,8 @@ public static class AssemblyLoader
 				if (assembly.GetName().Name != InAssemblyName.Name)
 					continue;
 
-				s_AssemblyCache[alcId].Add(assemblyId, assembly);
+				if (alcId.HasValue)
+					CacheAssembly(alcId.Value, assembly);
 				return assembly;
 			}
 		}
@@ -145,10 +173,11 @@ public static class AssemblyLoader
 
 		if ((resolved = tryResolve(AppContext.BaseDirectory)) != null) return resolved;
 
-		if (InAssemblyLoadContext != null && InAssemblyLoadContext.Name != null)
+		if (InAssemblyLoadContext != null &&
+			s_AssemblyContextIds.TryGetValue(InAssemblyLoadContext, out int resolvedContextId) &&
+			s_AlcDllPaths.TryGetValue(resolvedContextId, out var paths))
 		{
-			int alcId = InAssemblyLoadContext.Name.GetHashCode();
-			foreach (var path in s_AlcDllPaths[alcId])
+			foreach (var path in paths)
 			{
 				if ((resolved = tryResolve(path)) != null) return resolved;
 			}
@@ -160,24 +189,55 @@ public static class AssemblyLoader
 	[UnmanagedCallersOnly]
 	internal static int CreateAssemblyLoadContext(NativeString InName, NativeString InDllPath)
 	{
-		string? name = InName;
+		AssemblyLoadContext? alc = null;
+		int contextId = -1;
+		try
+		{
+			string? name = InName;
+			if (name == null)
+				return -1;
 
-		if (name == null)
+			contextId = Interlocked.Increment(ref s_NextAssemblyLoadContextId);
+			if (contextId <= 0)
+				throw new InvalidOperationException("AssemblyLoadContext IDs are exhausted.");
+			var paths = (InDllPath.ToString() ?? string.Empty).Split(
+				Path.PathSeparator, StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries);
+			alc = new AssemblyLoadContext(name, true);
+			alc.Resolving += ResolveAssembly;
+			alc.Unloading += ctx =>
+			{
+				s_AssemblyContextIds.Remove(ctx);
+				s_AssemblyContexts.Remove(contextId);
+				s_AssemblyCache.Remove(contextId);
+				s_AlcDllPaths.Remove(contextId);
+			};
+
+			s_AssemblyContexts.Add(contextId, alc);
+			s_AssemblyContextIds.Add(alc, contextId);
+			s_AssemblyCache.Add(contextId, new());
+			s_AlcDllPaths.Add(contextId, paths);
+			LogMessage($"Added ALC '{name}' with ID '{contextId}'", MessageLevel.Trace);
+			return contextId;
+		}
+		catch (Exception ex)
+		{
+			if (alc != null)
+			{
+				s_AssemblyContextIds.Remove(alc);
+				s_AssemblyContexts.Remove(contextId);
+				s_AssemblyCache.Remove(contextId);
+				s_AlcDllPaths.Remove(contextId);
+				try
+				{
+					alc.Unload();
+				}
+				catch
+				{
+				}
+			}
+			HandleException(ex);
 			return -1;
-
-		var alc = new AssemblyLoadContext(name, true);
-		alc.Resolving += ResolveAssembly;
-		alc.Unloading += ctx => s_AssemblyCache.Remove(ctx.Name!.GetHashCode());
-
-		int contextId = name.GetHashCode();
-		s_AssemblyContexts.Add(contextId, alc);
-		s_AssemblyCache.Add(contextId, new());
-
-		var path = InDllPath.ToString();
-		LogMessage($"Added ALC '{name}' with ID '{contextId}'", MessageLevel.Trace);
-		s_AlcDllPaths.Add(contextId, (path ?? "").Split(':'));
-
-		return contextId;
+		}
 	}
 
 	[UnmanagedCallersOnly]
@@ -249,6 +309,7 @@ public static class AssemblyLoader
 			BelongsToUnloadingContext(attribute.GetType().Assembly));
 
 		s_AssemblyContexts.Remove(InContextId);
+		s_AssemblyContextIds.Remove(alc);
 		s_AlcDllPaths.Remove(InContextId);
 		alc.Unload();
 	}
@@ -295,14 +356,15 @@ public static class AssemblyLoader
 
 			LogMessage($"Loading assembly '{InAssemblyFilePath}'", MessageLevel.Info);
 			var assemblyName = assembly.GetName();
-			int assemblyId = assemblyName.Name!.GetHashCode();
-			s_AssemblyCache[InContextId].Add(assemblyId, assembly);
+			int assemblyId = CacheAssembly(InContextId, assembly);
 			s_LastLoadStatus = AssemblyLoadStatus.Success;
 			return assemblyId;
 		}
 		catch (Exception ex)
 		{
-			s_AssemblyLoadErrorLookup.TryGetValue(ex.GetType(), out s_LastLoadStatus);
+			s_LastLoadStatus = s_AssemblyLoadErrorLookup.TryGetValue(ex.GetType(), out var status)
+				? status
+				: AssemblyLoadStatus.UnknownError;
 			HandleException(ex);
 			return -1;
 		}
@@ -336,14 +398,15 @@ public static class AssemblyLoader
 
 			LogMessage($"Loading assembly '{assembly.FullName}'", MessageLevel.Info);
 			var assemblyName = assembly.GetName();
-			int assemblyId = assemblyName.Name!.GetHashCode();
-			s_AssemblyCache[InContextId].Add(assemblyId, assembly);
+			int assemblyId = CacheAssembly(InContextId, assembly);
 			s_LastLoadStatus = AssemblyLoadStatus.Success;
 			return assemblyId;
 		}
 		catch (Exception ex)
 		{
-			s_AssemblyLoadErrorLookup.TryGetValue(ex.GetType(), out s_LastLoadStatus);
+			s_LastLoadStatus = s_AssemblyLoadErrorLookup.TryGetValue(ex.GetType(), out var status)
+				? status
+				: AssemblyLoadStatus.UnknownError;
 			HandleException(ex);
 			return -1;
 		}
@@ -355,7 +418,7 @@ public static class AssemblyLoader
 	[UnmanagedCallersOnly]
 	internal static NativeString GetAssemblyName(int InContextId, int InAssemblyId)
 	{
-		if (!s_AssemblyCache[InContextId].TryGetValue(InAssemblyId, out var assembly))
+		if (!TryGetAssembly(InContextId, InAssemblyId, out var assembly) || assembly == null)
 		{
 			LogMessage($"Couldn't get assembly name for assembly '{InAssemblyId}', assembly not in dictionary.", MessageLevel.Error);
 			return "";
diff --git a/Coral.Native/Include/Coral/Assembly.hpp b/Coral.Native/Include/Coral/Assembly.hpp
index 98b89a5..a00dca6 100644
--- a/Coral.Native/Include/Coral/Assembly.hpp
+++ b/Coral.Native/Include/Coral/Assembly.hpp
@@ -69,7 +69,7 @@ namespace Coral {
 		const StableVector<ManagedAssembly>& GetLoadedAssemblies() const { return m_LoadedAssemblies; }
 
 	private:
-		int32_t m_ContextId;
+		int32_t m_ContextId = -1;
 		StableVector<ManagedAssembly> m_LoadedAssemblies;
 
 		HostInstance* m_Host = nullptr;
diff --git a/Coral.Native/Include/Coral/HostInstance.hpp b/Coral.Native/Include/Coral/HostInstance.hpp
index 03848c9..5468398 100644
--- a/Coral.Native/Include/Coral/HostInstance.hpp
+++ b/Coral.Native/Include/Coral/HostInstance.hpp
@@ -53,7 +53,8 @@ namespace Coral {
 		AssemblyLoadContext CreateAssemblyLoadContext(std::string_view InName);
 		void UnloadAssemblyLoadContext(AssemblyLoadContext& InLoadContext);
 
-		// `InDllPath` is a colon-separated list of paths from which AssemblyLoader will try and resolve load paths at runtime.
+		// `InDllPath` is a platform-native path-list from which AssemblyLoader resolves dependencies at runtime
+		// (`;` on Windows and `:` on Unix).
 		// This does not affect the behaviour of LoadAssembly from native code.
 		AssemblyLoadContext CreateAssemblyLoadContext(std::string_view InName, std::string_view InDllPath);
 
diff --git a/Coral.Native/Source/Coral/Assembly.cpp b/Coral.Native/Source/Coral/Assembly.cpp
index 73477b3..f9bfa87 100644
--- a/Coral.Native/Source/Coral/Assembly.cpp
+++ b/Coral.Native/Source/Coral/Assembly.cpp
@@ -111,6 +111,7 @@ namespace Coral {
 		auto [idx, result] = m_LoadedAssemblies.EmplaceBack();
 		result.m_Host = m_Host;
 		result.m_AssemblyId = s_ManagedFunctions.LoadAssemblyFromMemoryFptr(m_ContextId, data, dataLength);
+		result.m_OwnerContextId = m_ContextId;
 		result.m_LoadStatus = s_ManagedFunctions.GetLastLoadStatusFptr();
 
 		if (result.m_LoadStatus == AssemblyLoadStatus::Success)
@@ -124,11 +125,18 @@ namespace Coral {
 			std::vector<TypeId> typeIds(static_cast<size_t>(typeCount));
 			s_ManagedFunctions.GetAssemblyTypesFptr(m_ContextId, result.m_AssemblyId, typeIds.data(), &typeCount);
 
+			result.m_LocalTypes.reserve(typeIds.size());
+			result.m_LocalTypeIdCache.reserve(typeIds.size());
+			result.m_LocalTypeNameCache.reserve(typeIds.size());
 			for (auto typeId : typeIds)
 			{
 				Type type;
 				type.m_Id = typeId;
 				result.m_Types.push_back(TypeCache::Get().CacheType(std::move(type)));
+
+				Type& inserted = result.m_LocalTypes.emplace_back(std::move(type));
+				result.m_LocalTypeIdCache[inserted.GetTypeId()] = &inserted;
+				result.m_LocalTypeNameCache[inserted.GetFullName()] = &inserted;
 			}
 		}
 
