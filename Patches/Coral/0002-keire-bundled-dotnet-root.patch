diff --git a/Coral.Native/Include/Coral/HostInstance.hpp b/Coral.Native/Include/Coral/HostInstance.hpp
--- a/Coral.Native/Include/Coral/HostInstance.hpp
+++ b/Coral.Native/Include/Coral/HostInstance.hpp
@@ -16,6 +16,11 @@ namespace Coral {
 		/// </summary>
 		std::string CoralDirectory;
 
+		/// <summary>
+		/// Optional application-owned .NET root containing host/fxr and shared/Microsoft.NETCore.App.
+		/// </summary>
+		std::string DotnetRoot;
+
 		MessageCallbackFn MessageCallback = nullptr;
 		MessageLevel MessageFilter = MessageLevel::All;
 
diff --git a/Coral.Native/Source/Coral/HostInstance.cpp b/Coral.Native/Source/Coral/HostInstance.cpp
--- a/Coral.Native/Source/Coral/HostInstance.cpp
+++ b/Coral.Native/Source/Coral/HostInstance.cpp
@@ -174,10 +174,12 @@ namespace Coral {
 #endif
 	}
 
-	static std::filesystem::path GetHostFXRPath()
+	static std::filesystem::path GetHostFXRPath(std::string_view InDotnetRoot)
 	{
+		const auto dotnetRoot = Utf8Path(InDotnetRoot);
 		size_t size = 0;
-		get_hostfxr_parameters parameters { sizeof(get_hostfxr_parameters), nullptr, nullptr };
+		get_hostfxr_parameters parameters {
+			sizeof(get_hostfxr_parameters), nullptr, dotnetRoot.empty() ? nullptr : dotnetRoot.c_str() };
 		(void)get_hostfxr_path(nullptr, &size, &parameters);
 		if (size == 0)
 			return {};
@@ -205,7 +207,7 @@ namespace Coral {
 	{
 		if (m_HostFXRLibrary != nullptr)
 			return true;
-		const auto hostfxrPath = GetHostFXRPath();
+		const auto hostfxrPath = GetHostFXRPath(m_Settings.DotnetRoot);
 		if (hostfxrPath.empty())
 			return false;
 
