diff --git a/Coral.Native/Include/Coral/Core.hpp b/Coral.Native/Include/Coral/Core.hpp
--- a/Coral.Native/Include/Coral/Core.hpp
+++ b/Coral.Native/Include/Coral/Core.hpp
@@ -65,8 +65,8 @@
 // TODO(Emily): Make a better system for supported version ranges (See `HostInstance.cpp:GetHostFXRPath()`)
 /*
 #define CORAL_DOTNET_TARGET_VERSION_MAJOR 8
 #define CORAL_DOTNET_TARGET_VERSION_MAJOR_STR '8'
 */
-#define CORAL_UNMANAGED_CALLERS_ONLY ((const UCChar*) (-1ULL))
+#define CORAL_UNMANAGED_CALLERS_ONLY reinterpret_cast<const UCChar*>(UINTPTR_MAX)
 
 namespace Coral {
diff --git a/Coral.Native/Source/Coral/Memory.cpp b/Coral.Native/Source/Coral/Memory.cpp
--- a/Coral.Native/Source/Coral/Memory.cpp
+++ b/Coral.Native/Source/Coral/Memory.cpp
@@ -34,7 +34,8 @@ namespace Coral {
 		if (buffer != nullptr)
 		{
 			memset(buffer, 0xCE, size);
-			wcscpy(buffer, InString.data());
+			memcpy(buffer, InString.data(), InString.size() * sizeof(UCChar));
+			buffer[InString.size()] = {};
 		}
 #else
 		UCChar* buffer;
@@ -43,7 +44,7 @@ namespace Coral {
 
 		if ((buffer = static_cast<UCChar*>(calloc(length, sizeof(UCChar)))))
 		{
-			strcpy(buffer, InString.data());
+			memcpy(buffer, InString.data(), InString.size() * sizeof(UCChar));
 		}
 #endif
 
diff --git a/cmake/CMakeLists.txt b/cmake/CMakeLists.txt
--- a/cmake/CMakeLists.txt
+++ b/cmake/CMakeLists.txt
@@ -77,6 +77,11 @@ add_dependencies(Coral.Native CoralManaged)
 
 set_target_properties(Coral.Native PROPERTIES LINKER_LANGUAGE CXX)
 target_compile_features(Coral.Native PUBLIC cxx_std_17)
+if(MSVC)
+	# Coral still implements deprecated global TypeCache compatibility APIs internally. Keep that intentional
+	# compatibility surface from obscuring actionable dependency-build warnings.
+	target_compile_options(Coral.Native PRIVATE /wd4996)
+endif()
 
 target_precompile_headers(Coral.Native PRIVATE ${CORAL_ROOT}/Coral.Native/Source/CoralPCH.hpp)
 
