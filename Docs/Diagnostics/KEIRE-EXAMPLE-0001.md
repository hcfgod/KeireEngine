# KEIRE-EXAMPLE-0001: Example module diagnostic

This definition is registered by the SDK source-module example. It demonstrates that diagnostic definitions join the
application catalog transactionally before the catalog is frozen and can resolve packaged or online documentation.
